declare class nodePortCheck {
    net: any;
    defaultOptions: any;
    options: any;
    server: any;
    retryServer: number;
    portAvailable: boolean;
    savePorts: any;
    port: number;
    ports: any;
    callback: any;
    isServerBound: boolean;
    serverErrors: string[];
    constructor(options: any, callback: (isPortAvailable: boolean, availablePort: number, initialPort: number) => {});
    private setCurrentPort();
    private output(...input);
    private canRetry();
    private finalCallback();
    private getHostAndPort();
    createServer(): void;
}
