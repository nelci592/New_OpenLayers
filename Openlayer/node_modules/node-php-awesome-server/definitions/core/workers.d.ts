import { NPAS_Options } from "../index";
import { PHPServer } from "./phpserver";
import { Utils } from "./utils";
export declare class Workers {
    npas: {
        [key: number]: PHPServer;
    };
    utils: Utils;
    constructor(options: NPAS_Options);
    private static createPHPInstance(createPort, options, cluster, instanceNr);
}
