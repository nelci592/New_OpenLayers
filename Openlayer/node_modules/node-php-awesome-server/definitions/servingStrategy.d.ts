/// <reference types="express" />
/// <reference types="node" />
import { Request } from "express";
import { IncomingMessage } from "http";
export declare type TPorts = number[];
export declare type TObject = {
    [key: string]: any;
};
export declare type TObjectArray = {
    [key: string]: TObject[];
};
export declare class ServingStrategy {
    ports: TPorts;
    private _activity;
    private _portsInUse;
    private _currentPort;
    private _activeRequests;
    private utils;
    constructor(portList?: TPorts);
    getPort(): number;
    activityRequest(req: Request): TObject;
    activityResponse(res: IncomingMessage, activityObject: TObject): TObject;
    updatePorts(portList: TPorts): void;
}
