import { NPAS_Options } from "../index";
import { Utils } from "./utils";
export declare class PHPServer {
    workerId: number;
    workerPid: number;
    defaultOptions: any;
    options: any;
    args: string[];
    events: any;
    emitter: any;
    php: any;
    utils: Utils;
    constructor(options: NPAS_Options, workerId: number, workerPid: number, instanceNr: number);
    /**
     * Listener
     * @param event
     * @param callback
     */
    on(event: string, callback: (...input: any[]) => void): this;
    /**
     * Format the parameters used for the PHP server and returns an array of arguments
     * @returns {string[] | number[]}
     */
    prepareArguments(): string[] | number[];
    /**
     * Retrieve a valid format of host and port
     * @returns {string}
     */
    private getBindAddress();
    /**
     * Check if the PHP server has started and emit 'connect' Event
     * @returns {void}
     */
    phpCheck(): void;
}
